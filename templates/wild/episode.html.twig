{% extends 'base.html.twig' %}

{% block title %}{{ episode.title }}{% endblock %}

{% block body %}

    <div class="media">
        <img class="align-self-start mr-3" src="{{program.poster}}" alt="{{ program.title }} poster">
        <div class="media-body">
            <a href="{{ path('wild_show', {'slug': program.slug }) }}"><h1 class="mt-0">{{ program.title|capitalize }}</h1></a>
            <p>Categorie : {{ program.category.name }}</p>
            <a href="{{ path('wild_season', {'id': season.id}) }}">
                <h2>Saison {{ season.number }}</h2>
            </a>
                <h3>Episode {{ episode.number }} : {{ episode.title|capitalize }}</h3>
                <p>{{ episode.synopsis }}</p>

            <a href="{{ path('wild_programs') }}">
                Retour à l'accueil
            </a>
        </div>
    </div>
{% endblock %} 